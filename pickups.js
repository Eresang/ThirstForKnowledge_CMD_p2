var amountOfPickups = 30;
var pickupArray = [];

function addPickupsToArray () {
    for (i = 0; i < amountOfPickups; i++) {
        pickup = game.add.sprite(0, 0, "");
    }
}

function spawnPickup () {
    
}